//lien generique pour le prenom + le graphique score + les 4 tags
"http://localhost:3001/user/${userId}"

//lien pour le graphique Activités
"http://localhost:3001/user/${userId}/activity"

//lien pour le graphique durée moyenne des sessions
"http://localhost:3001/user/${userId}/average-sessions"

// lien pour le graphique du radar avec les types de performances
"http://localhost:3001/user/${userId}/performance"
